# SEO Platform 使用手册

> **一体化 SEO 运营平台**  
> 关键词研究 · AI 写作 · 站内优化 · 社交分发 · 排名监控 · 数据报告

---

## 目录

1. [系统简介](#一系统简介)
2. [环境搭建](#二环境搭建)
3. [首次使用与账号体系](#三首次使用与账号体系)
4. [数据与权限](#四数据与权限)
5. [功能模块详解](#五功能模块详解)
6. [标准工作流](#六标准工作流)
7. [API 参考](#七api-参考)
8. [运维与排错](#八运维与排错)
9. [目录结构](#九目录结构)

---

## 一、系统简介

### 1.1 产品定位

SEO Platform 是一款 **面向单站 SEO 运营** 的一体化管理平台。登录后系统自动关联默认工作空间，关键词、文章、排名、外链、GSC 数据均按用户隔离，开箱即用，无需手动创建或切换客户站点。

### 1.2 核心能力

| 能力 | 说明 |
|------|------|
| 内容创作 | AI 辅助写作、平台内草稿、同步到 WordPress |
| 外部页面分析 | 抓取任意 URL 做 SEO 检查或批量审计 |
| 排名监控 | Google SERP 定时抓取，TimescaleDB 存储趋势 |
| Search Console | Google OAuth 授权与搜索表现数据查看 |
| 社交分发 | PulseForge 多平台发帖与引流统计 |
| 策略引擎 | 自动生成优化工单 |
| 数据报告 | 一键导出月度 Markdown SEO 报告 |

### 1.3 功能模块与路由

| 模块 | 页面路由 | 说明 |
|------|----------|------|
| 综合大屏 | `/dashboard` | 统计卡片、图表、待办建议、导出报告 |
| 文章创作 | `/articles` `/articles/new` `/articles/:id` | 列表、新建、编辑、CMS 同步 |
| SEO 检查 | `/seo-checker` | 单页检查、URL 抓取、批量 Sitemap 审计 |
| 社交分发 | `/social` | 账号绑定、发帖调度 |
| 排名监控 | `/serp` | 关键词排名、趋势、手动抓取 |
| Search Console | `/search-console` | GSC 授权与数据分析 |
| 外链监控 | `/backlinks` | 外链新增/丢失/存活检测 |
| 竞品对标 | `/competitors` | 竞品域名排名对比 |
| 优化建议 | `/recommendations` | 策略引擎工单 |

### 1.4 技术架构

```
浏览器 (Vue3 :5173)
    │  Header: Authorization
    ▼
FastAPI (:8000)
    ├── PostgreSQL + TimescaleDB（业务数据 + 排名时序）
    ├── Redis（Celery Broker）
    └── Celery Worker / Beat（SERP 抓取、社交发帖、定时告警）
            │
            ▼
外部服务：DeepSeek API / ScrapingBee或代理池 / PulseForge / Google GSC OAuth
```

### 1.5 数据隔离原则

- 登录后系统自动为用户创建或关联 **默认站点**（`client_sites` 表），业务数据通过内部 `site_id` 隔离。
- 前端 **无需** 传递 `X-Site-Id` 请求头，后端自动解析当前用户的默认站点。
- 关键词落地页、外链目标 URL、SEO 检查 URL **不再校验域名**，可填写任意有效 URL。

---

## 二、环境搭建

### 2.1 前置要求

| 组件 | 版本要求 |
|------|----------|
| Python | 3.11+（推荐 Conda 环境名 `SEO`） |
| Node.js | 18+ |
| Docker | 用于 PostgreSQL + Redis |
| Conda | 管理 Python 依赖 |

### 2.2 启动数据库

```bash
cd c:\SEO
docker compose up -d
# 等待 PostgreSQL 和 Redis 就绪（约 30 秒）
```

### 2.3 初始化数据库

```bash
cd c:\SEO\backend
conda run -n SEO python -m alembic upgrade head
```

迁移将创建：`users`、`client_sites`、`site_members`、`keywords`、`articles`、`serp_rank_snapshots`、`gsc_connections`、`seo_audits` 等表。  
首次调用业务 API 时，系统会自动为当前用户创建「默认站点」。

### 2.4 配置环境变量

复制并编辑 `backend/.env`（参考 `backend/.env.example`）：

```env
# 应用
SECRET_KEY=请替换为随机强密钥
FRONTEND_URL=http://127.0.0.1:5173

# DeepSeek AI（AI 写作必填）
DEEPSEEK_API_KEY=sk-xxxxxxxx
DEEPSEEK_MODEL=deepseek-chat

# SERP 抓取（二选一或同时配置，优先 ScrapingBee）
SCRAPINGBEE_API_KEY=xxxxxxxx
PROXY_LIST=http://user:pass@proxy:port

# PulseForge（社交分发，可选）
PULSEFORGE_API_BASE=https://api.pulseforge.example.com
PULSEFORGE_API_KEY=pf_xxxxxxxx

# Google Search Console OAuth（可选）
GOOGLE_CLIENT_ID=xxxxxxxx.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=xxxxxxxx
GOOGLE_REDIRECT_URI=http://127.0.0.1:8000/api/v1/gsc/oauth/callback
```

### 2.5 启动后端

```bash
cd c:\SEO\backend
conda run -n SEO python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

- Swagger 文档：`http://127.0.0.1:8000/docs`
- 健康检查：访问 `/docs` 返回 200 即表示后端正常

### 2.6 启动 Celery Worker（新终端）

```bash
cd c:\SEO\backend
conda run -n SEO celery -A app.core.celery_app worker --loglevel=info --pool=solo -Q default,serp,social
```

负责：SERP 排名抓取、社交发帖、外链/告警定时任务。

### 2.7 启动 Celery Beat（新终端，可选）

```bash
cd c:\SEO\backend
conda run -n SEO celery -A app.core.celery_app beat --loglevel=info
```

负责：定时触发 SERP 抓取（默认约每 6 小时）、策略引擎扫描等。

### 2.8 启动前端

```bash
cd c:\SEO\frontend
npm install
npm run dev
```

访问 `http://127.0.0.1:5173`，未登录将跳转登录页。

---

## 三、首次使用与账号体系

### 3.1 注册与登录

**方式一：前端页面**

1. 打开 `http://127.0.0.1:5173/login`
2. 「注册新账号」：填写邮箱（必填）、昵称（选填）、密码（至少 8 位）
3. 注册成功后切换「登录」Tab 登录

**方式二：API**

```http
POST /api/v1/auth/register
Content-Type: application/json

{
  "email": "admin@example.com",
  "password": "your_password_8+",
  "full_name": "运营管理员"
}
```

```http
POST /api/v1/auth/login
Content-Type: application/json

{
  "email": "admin@example.com",
  "password": "your_password_8+"
}
```

响应示例：

```json
{
  "access_token": "eyJhbGci...",
  "token_type": "bearer",
  "expires_in": 86400
}
```

### 3.2 Token 存储

登录成功后前端自动将 Token 写入：

| Key | 说明 |
|-----|------|
| `localStorage.access_token` | JWT，后续请求 Authorization 头 |
| `localStorage.user_email` | 展示用邮箱 |

### 3.3 登录后即用

登录成功后即可直接使用各功能模块，**无需** 手动创建或选择客户站点：

1. 首次访问业务 API 时，后端自动创建名为「默认站点」的工作空间
2. 所有关键词、文章、排名等数据自动归属该默认站点
3. 直接进入 **综合大屏**、**排名监控** 等页面开始操作

### 3.4 CMS 与 Sitemap 配置（可选）

WordPress 同步、批量 SEO 审计依赖站点级配置（`client_sites` 表）。当前版本已移除前端管理页面，可通过数据库直接更新默认站点：

```sql
UPDATE client_sites
SET cms_type = 'wordpress',
    cms_api_url = 'https://your-site.com',
    cms_api_key = 'editor_username:xxxx xxxx xxxx',
    sitemap_url = 'https://your-site.com/sitemap.xml',
    domain = 'your-site.com'
WHERE owner_user_id = (SELECT id FROM users WHERE email = 'your@email.com');
```

---

## 四、数据与权限

### 4.1 内部站点模型

系统在数据库中保留 `client_sites` 表作为数据隔离单元，但对用户透明：

| 字段 | 说明 |
|------|------|
| `name` | 站点显示名称（默认「默认站点」） |
| `domain` | 主域名，用于报告导出命名等 |
| `cms_type` | CMS 类型：`none` / `wordpress` / `shopify` / `custom` |
| `cms_api_url` | WordPress 站点根地址 |
| `cms_api_key` | WordPress 应用密码，格式 `用户名:xxxx xxxx xxxx` |
| `sitemap_url` | 用于 SEO 批量审计抓取 URL 列表 |

### 4.2 成员角色（内部）

| 角色 | 代码 | 权限 |
|------|------|------|
| 管理员 | `admin` | 全部读写（默认角色） |
| 运营专员 | `operator` | 关键词/文章/监控等读写 |
| 只读 | `client_viewer` | 仅查看数据，不可修改 |

> 当前版本已移除站点成员管理界面；新注册用户自动获得默认站点的 `admin` 角色。

### 4.3 API 请求头规范

业务请求仅需携带：

```http
Authorization: Bearer {access_token}
```

`X-Site-Id` 为可选项；未传时后端自动使用当前用户的默认站点。

---

## 五、功能模块详解

### 5.1 综合大屏（`/dashboard`）

**用途**：核心 KPI 一览与决策入口。

**统计卡片（6 张）**

| 卡片 | 含义 |
|------|------|
| 本周新发布文章 | 周期内 `published` 文章数及环比 |
| 关键词 Top10 占比 | 监控词进入前 10 名的比例 |
| 社交引流点击 | PulseForge 回流点击总量 |
| 监控关键词数 | 状态为 `active` 的关键词数 |
| 外链净增 | 新增外链 − 丢失外链 |
| 待处理建议数 | 状态为 `open` 的优化工单数 |

**图表**

- **关键词排名趋势**：近 N 天排名曲线（Y 轴越小越好）
- **社交引流漏斗**：发布文章 → 发帖数 → 已发布帖 → 点击量
- **外链统计**：存活 vs 失效饼图

**工具栏**

| 按钮 | 说明 |
|------|------|
| 时间范围 | 7 / 14 / 30 / 90 天 |
| 重新生成建议 | 手动触发策略引擎 |
| 导出月度报告 | 下载 Markdown 格式客户报告 |
| 刷新 | 重新拉取全部图表数据 |

**API**

```
GET  /api/v1/dashboard/summary?days=7
GET  /api/v1/dashboard/rank-trends?days=14
GET  /api/v1/dashboard/social-funnel?days=7
GET  /api/v1/dashboard/backlink-stats?days=30
GET  /api/v1/reports/monthly?days=30
GET  /api/v1/reports/monthly/export?days=30
```

---

### 5.2 文章创作（`/articles`）

**用途**：撰写 SEO 文章，或从外部 URL 导入页面进行分析。

#### 5.2.1 文章类型

| 类型 | `source` 字段 | 说明 |
|------|---------------|------|
| 平台创作 | `internal` | 在系统内编写，可同步到 WordPress CMS |
| 外部页面 | `external` | 从任意 URL 抓取导入，用于分析优化 |

#### 5.2.2 文章状态机

```
draft ──→ ai_generated ──→ reviewed ──→ published
  ↑            │                │
  └────────────┴────────────────┘（可回退 draft）
published ──→ reviewed（下架复审）
```

| 状态 | 含义 |
|------|------|
| `draft` | 草稿 |
| `ai_generated` | AI 已生成初稿，待人工审阅 |
| `reviewed` | 已审核，可发布或同步 CMS |
| `published` | 已发布（记录 `published_at`） |

#### 5.2.3 编辑器功能

**左栏 — AI 写作**

- 选择模型：DeepSeek（默认）/ Agnes
- 输入：核心关键词、大纲（选填）、受众、语气
- 点击「AI 生成文章」：调用 DeepSeek，填充标题、Meta、正文
- 「AI 生成 Meta」：根据正文重新生成 Meta Description

**中栏 — Markdown 编辑器**

- 支持实时预览
- Ctrl+S 触发保存

**右栏 — SEO 检查**

- 对当前内容运行 SEO 检查
- 显示总分、分项得分、AI 检测分、CWV 预估

**顶部工具栏**

| 按钮 | 说明 |
|------|------|
| 保存 | 创建或更新文章 |
| 切换状态 | 按状态机流转 |
| 同步到客户站 | 将文章推送到 WordPress（需在数据库配置 CMS，见 [3.4](#34-cms-与-sitemap-配置可选)） |

#### 5.2.4 从外部 URL 导入页面

```http
POST /api/v1/articles/import-url?url=https://example.com/blog/post-slug
Authorization: Bearer {token}
```

- 支持任意可访问的 URL，无域名限制
- 系统自动抓取 Title、Meta、正文，创建 `source=external` 记录

#### 5.2.5 CMS 同步（WordPress）

```http
POST /api/v1/articles/{id}/sync-cms
```

- 新建：在 WordPress 创建文章
- 已同步（有 `cms_post_id`）：更新已有文章
- 成功后更新 `source_url`、`sync_status=synced`

**API 汇总**

```
GET    /api/v1/articles?status=&keyword_id=&source=&page=&size=
POST   /api/v1/articles
POST   /api/v1/articles/import-url?url=
GET    /api/v1/articles/{id}
PATCH  /api/v1/articles/{id}
POST   /api/v1/articles/{id}/status        { "status": "published" }
POST   /api/v1/articles/{id}/sync-cms
DELETE /api/v1/articles/{id}
```

---

### 5.3 AI 写作（文章编辑器内 + API）

**用途**：根据关键词快速生成符合 SEO 结构的长文初稿。

**生成内容包含**

- 标题（含关键词）
- Meta Description（150–160 字符建议）
- Markdown 正文（H1/H2 层级）
- 推荐相关关键词列表

**API**

```
POST /api/v1/ai/generate              仅生成，不入库
POST /api/v1/ai/generate-and-save     生成并入库，状态 ai_generated
POST /api/v1/ai/meta                  重新生成 Meta Description
POST /api/v1/ai/social-copy           生成社交平台差异化文案
```

**`generate` 请求示例**

```json
{
  "keyword": "best python seo tools",
  "outline": "1. 引言\n2. 工具对比\n3. 总结",
  "audience": "技术从业者和内容创作者",
  "tone": "专业、务实、易懂",
  "model": "deepseek"
}
```

**依赖**：`DEEPSEEK_API_KEY` 必须有效且有余额。

---

### 5.4 SEO 检查器（`/seo-checker`）

**用途**：对内容进行静态 SEO 分析，支持手动输入、已入库文章、任意 URL 抓取、全站批量审计。

#### 5.4.1 Tab：单页检查

**输入项**

- 选择已入库文章（自动填充）
- 标题、关键词、Meta Description、正文（Markdown）、封面图 URL

**检查项与评分标准**

| 检查项 | 标准 |
|--------|------|
| Title | 理想 50–60 字符，含关键词 |
| Meta Description | 理想 150–160 字符 |
| 关键词密度 | 1%–3% |
| 标题层级 | H1/H2/H3 结构合理，含关键词 |
| 图片 Alt | 正文图片是否有 alt 文本 |
| 内容长度 | 建议 ≥ 800 字 |
| 内链建议 | 与同站其他文章的相关性（Jaccard） |

**附加指标**

- **AI 内容检测分**：0–100，越高越像 AI 生成（建议 < 30）
- **Core Web Vitals 预估**：LCP / CLS / INP 风险等级

**对已入库文章写回得分**

```http
POST /api/v1/seo/check/{article_id}
{ "keyword": "目标关键词" }
```

写回字段：`articles.seo_score`、`seo_detail`、`ai_detected_score`

#### 5.4.2 Tab：URL 抓取检查

- 输入任意页面 URL
- 系统抓取页面 HTML，提取 Title/Meta/正文后评分
- 适合审计 **已有页面** 而无需手动复制内容

```http
POST /api/v1/seo/check-url
{ "url": "https://example.com/page", "keyword": "可选" }
```

#### 5.4.3 Tab：批量审计

- 读取默认站点配置的 `sitemap_url`（见 [3.4](#34-cms-与-sitemap-配置可选)）
- 解析 Sitemap XML（支持索引型 Sitemap）
- 逐页抓取并评分，生成审计报告

| 字段 | 说明 |
|------|------|
| `total_pages` | 计划扫描页数上限 |
| `scanned_pages` | 实际完成数 |
| `avg_score` | 平均分 |
| `summary` | 文字摘要 |
| `pages[]` | 每页 URL、得分、问题数 |

```http
POST /api/v1/seo/audits        { "max_pages": 50 }
GET  /api/v1/seo/audits
GET  /api/v1/seo/audits/{id}
```

#### 5.4.4 内链推荐

```http
GET /api/v1/seo/internal-links/{article_id}?limit=5
```

基于标题与正文关键词重合度（Jaccard）推荐站内互链，并写入 `internal_links` 表。

---

### 5.5 排名监控（`/serp`）

**用途**：跟踪目标关键词在 Google 的排名变化。

#### 5.5.1 添加关键词

| 字段 | 说明 |
|------|------|
| 关键词 | 如 `best python seo tools` |
| 落地页 URL | 选填，可填写任意 URL |
| 搜索引擎 | 默认 `google` |
| 地区 | 默认 `us` |
| 优先级 | 1（最高）– 5（最低） |

#### 5.5.2 排名数据

- **最新排名**：各关键词当前位次
- **趋势图**：多关键词时间曲线（TimescaleDB `time_bucket` 聚合）
- **抓取模式**：页面顶部显示 `mock` / `scrapingbee` / `proxy`

#### 5.5.3 抓取机制

- **定时**：Celery Beat 约每 6 小时自动抓取
- **手动**：点击「立即抓取」或 `POST /api/v1/serp/crawl/{keyword_id}`
- **存储**：`serp_rank_snapshots` 时序表（复合主键 `time + keyword_id`）

#### 5.5.4 代理配置优先级

1. `SCRAPINGBEE_API_KEY` 已配置 → 使用 ScrapingBee Google API
2. `PROXY_LIST` / `PROXY_ENDPOINT` 已配置 → 住宅代理直连 Google
3. 均未配置 → Mock 模式（随机排名，仅供开发）

**API**

```
GET  /api/v1/serp/config
GET  /api/v1/serp/latest
GET  /api/v1/serp/snapshots?keyword_id=&page=&size=
GET  /api/v1/serp/trends?keyword_ids=1,2&days=30
POST /api/v1/serp/crawl/{keyword_id}
```

---

### 5.6 Search Console（`/search-console`）

**用途**：连接 Google Search Console，查看真实搜索表现。

#### 5.6.1 前置配置

1. [Google Cloud Console](https://console.cloud.google.com/) 创建项目
2. 启用 **Google Search Console API**
3. 创建 OAuth 2.0 Web 客户端
4. 授权重定向 URI：`http://127.0.0.1:8000/api/v1/gsc/oauth/callback`
5. 将 Client ID / Secret 写入 `backend/.env`

#### 5.6.2 连接流程

1. 进入 Search Console 页面
2. 点击「连接 Google 账号」→ 浏览器跳转 Google 授权
3. 授权成功后回调，页面显示已连接
4. 在下拉框选择 GSC 中的属性（域名或 URL 前缀）
5. 查看汇总指标与关键词/页面明细

#### 5.6.3 数据说明

| 指标 | 含义 |
|------|------|
| 点击 | 搜索结果中被点击次数 |
| 展示 | 出现在搜索结果中的次数 |
| CTR | 点击率 |
| 平均排名 | Google 报告的平均位置 |

数据有约 **2 天延迟**（系统内置 `GSC_DATA_LAG_DAYS=2`）。

**API**

```
GET    /api/v1/gsc/status
GET    /api/v1/gsc/oauth/start
DELETE /api/v1/gsc/disconnect
GET    /api/v1/gsc/sites
PUT    /api/v1/gsc/sites/active          { "site_url": "sc-domain:example.com" }
GET    /api/v1/gsc/analytics/summary?days=28
GET    /api/v1/gsc/analytics?days=28&dimension=query|page&row_limit=100
```

---

### 5.7 外链监控（`/backlinks`）

**用途**：跟踪外部链接，检测新增与丢失。

#### 5.7.1 添加外链记录

| 字段 | 说明 |
|------|------|
| 目标 URL | 被链接的页面（任意 URL） |
| 来源 URL | 引用该链接的外部页面 |
| 锚文本 | 选填 |
| 域名权重 DA | 选填，0–100 |

#### 5.7.2 存活检测

- **单条检测**：`POST /api/v1/backlinks/{id}/check`
- **批量检测**：`POST /api/v1/backlinks/check-all?limit=100`
- 检测方式：HTTP HEAD 请求（Mock 模式下 90% 存活率模拟）

#### 5.7.3 大屏统计

- 总外链数、存活数、新增、丢失、净增、存活率

**API**

```
GET    /api/v1/backlinks?is_alive=&page=&size=
POST   /api/v1/backlinks
PATCH  /api/v1/backlinks/{id}
DELETE /api/v1/backlinks/{id}
POST   /api/v1/backlinks/{id}/check
POST   /api/v1/backlinks/check-all
```

---

### 5.8 竞品对标（`/competitors`）

**用途**：监控竞争对手域名在同一批关键词下的排名。

#### 5.8.1 添加竞品

- **域名**：如 `competitor.com`（不可重复）
- **名称**：显示用别名

#### 5.8.2 排名对比

- SERP 抓取时同步记录竞品在相同关键词下的排名
- 竞品详情页可查看各关键词排名趋势

**API**

```
GET    /api/v1/competitors
POST   /api/v1/competitors
DELETE /api/v1/competitors/{id}
GET    /api/v1/competitors/{id}/ranks?days=30
```

---

### 5.9 社交分发（`/social`）

**用途**：将文章推广到 LinkedIn、Twitter/X、Facebook 等平台（经 PulseForge 适配层）。

#### 5.9.1 绑定社交账号

| 字段 | 说明 |
|------|------|
| 平台 | `pulseforge` / `linkedin` / `twitter` / `facebook` |
| 账号名称 | 显示名 |
| Access Token | 平台 API 凭证 |
| 是否启用 | `is_active` |

#### 5.9.2 创建发帖任务

- 关联文章（选填）
- 选择社交账号
- 标题、摘要、配图、外链 URL、Hashtags
- 定时发布时间（选填，留空则 pending）

#### 5.9.3 发帖状态

`pending` → `scheduled` → `posting` → `posted` / `failed`

- **立即发送**：`POST /api/v1/social/posts/{id}/send-now`
- Celery 定时任务在 `scheduled_at` 自动发送

#### 5.9.4 一键联动分发

文章发布后调用：

```http
POST /api/v1/social/auto-distribute
{
  "article_id": 1,
  "delay_minutes": 0
}
```

系统为每个激活账号：AI 生成差异化文案 → 创建定时帖 → 投递 Celery（每账号错峰 5 分钟）。

**API**

```
GET    /api/v1/social/accounts
POST   /api/v1/social/accounts
PATCH  /api/v1/social/accounts/{id}
DELETE /api/v1/social/accounts/{id}
GET    /api/v1/social/posts
POST   /api/v1/social/posts
PATCH  /api/v1/social/posts/{id}
DELETE /api/v1/social/posts/{id}
POST   /api/v1/social/posts/{id}/send-now
POST   /api/v1/social/auto-distribute
```

---

### 5.10 社区互动（API）

**用途**：为文章生成 Reddit / PulseForge 等平台的「像真人」评论草稿，供运营人工修改后发布。

```http
POST /api/v1/community/comments-draft
{
  "article_id": 1,
  "platforms": ["Reddit", "PulseForge"],
  "count": 3
}
```

---

### 5.11 优化建议（`/recommendations`）

**用途**：策略引擎根据排名、社交、SEO 得分等数据自动生成工单。

#### 5.11.1 内置规则

| 规则 | 触发条件 | 严重程度 | 建议操作 |
|------|----------|----------|----------|
| 排名不佳 | 发布 ≥7 天且排名 > 50 | Warning | 再推社交 + Reddit 互动 |
| 社交零互动 | 已发帖但总互动 = 0 | Info | 改配图 / 标题加悬念 |
| SEO 得分低 | 得分 < 60 | Warning | 完善 Title/Meta/H2/Alt |
| 爬虫异常 | 7 天无 SERP 快照 | Critical | 检查代理池 / Worker |

#### 5.11.2 工单状态

| 状态 | 说明 |
|------|------|
| `open` | 待处理 |
| `in_progress` | 处理中 |
| `resolved` | 已解决 |
| `ignored` | 已忽略 |

#### 5.11.3 页面操作

- 按严重程度、分类、状态筛选
- 标记「处理中」/「已解决」/「忽略」
- 大屏或本页「重新生成建议」手动触发扫描

**API**

```
GET    /api/v1/dashboard/recommendations?category=&severity=&status=&page=&size=
POST   /api/v1/dashboard/recommendations/generate?days_lookback=7
PATCH  /api/v1/dashboard/recommendations/{id}    { "status": "resolved" }
DELETE /api/v1/dashboard/recommendations/{id}
```

---

### 5.12 月度 SEO 报告

**用途**：导出阶段性 SEO 成果报告。

**包含指标**

- 监控关键词数、Top10 排名次数
- 新发布文章数
- 外链新增/丢失/净增
- 待处理优化建议数

**导出方式**

1. 大屏 →「导出月度报告」按钮
2. API 下载 Markdown：

```http
GET /api/v1/reports/monthly/export?days=30
```

报告文件命名：`seo-report-{domain}-{days}d.md`

---

## 六、标准工作流

### 6.1 首次接入（约 20 分钟）

```
1. 注册登录 → 系统自动创建默认工作空间
2. （可选）数据库配置 CMS / Sitemap
3. Search Console → OAuth 授权并选择 GSC 属性
4. 排名监控 → 导入核心关键词
5. 竞品对标 → 添加 2–3 个竞品域名
6. SEO 检查 → 批量审计 Sitemap（了解全站健康度基线）
```

### 6.2 月度内容 + 优化闭环

```
1. 文章创作 → AI 生成初稿 → 人工审阅 → 发布
2. 同步到 WordPress → CMS 一键推送（需提前配置）
3. SEO 检查器 → 确认得分 ≥ 60
4. 社交分发 → 一键联动或定时发帖
5. 排名监控 → 观察 7–14 天趋势
6. 优化建议 → 处理策略引擎工单
7. 综合大屏 → 导出月度报告
```

### 6.3 纯监控型（无内容代写）

```
1. 配置 GSC + 关键词
2. 定期 SEO 批量审计 + URL 单页检查
3. 外链监控存活检测
4. 大屏查看数据 + 导出报告
```

---

## 七、API 参考

> 完整交互式文档：`http://127.0.0.1:8000/docs`

### 7.1 认证

```
POST /api/v1/auth/register     { email, password, full_name? }
POST /api/v1/auth/login        { email, password } → { access_token, token_type, expires_in }
GET  /api/v1/auth/me           当前用户信息
```

### 7.2 关键词

```
GET    /api/v1/keywords
POST   /api/v1/keywords
DELETE /api/v1/keywords/{id}
```

### 7.3 文章

```
GET    /api/v1/articles
POST   /api/v1/articles
POST   /api/v1/articles/import-url?url=
GET    /api/v1/articles/{id}
PATCH  /api/v1/articles/{id}
POST   /api/v1/articles/{id}/status
POST   /api/v1/articles/{id}/sync-cms
DELETE /api/v1/articles/{id}
```

### 7.4 AI 写作

```
POST /api/v1/ai/generate
POST /api/v1/ai/generate-and-save
POST /api/v1/ai/meta
POST /api/v1/ai/social-copy
```

### 7.5 SEO 检查

```
POST /api/v1/seo/check
POST /api/v1/seo/check-url
POST /api/v1/seo/check/{article_id}
GET  /api/v1/seo/internal-links/{article_id}
POST /api/v1/seo/audits
GET  /api/v1/seo/audits
GET  /api/v1/seo/audits/{id}
```

### 7.6 排名监控

```
GET  /api/v1/serp/config
GET  /api/v1/serp/latest
GET  /api/v1/serp/snapshots
GET  /api/v1/serp/trends
POST /api/v1/serp/crawl/{keyword_id}
```

### 7.7 Search Console

```
GET    /api/v1/gsc/status
GET    /api/v1/gsc/oauth/start
GET    /api/v1/gsc/oauth/callback
DELETE /api/v1/gsc/disconnect
GET    /api/v1/gsc/sites
PUT    /api/v1/gsc/sites/active
GET    /api/v1/gsc/analytics/summary
GET    /api/v1/gsc/analytics
```

### 7.8 外链 / 竞品 / 社交 / 社区

```
# 外链
GET/POST/PATCH/DELETE /api/v1/backlinks[...]
POST /api/v1/backlinks/{id}/check
POST /api/v1/backlinks/check-all

# 竞品
GET/POST /api/v1/competitors
DELETE /api/v1/competitors/{id}
GET  /api/v1/competitors/{id}/ranks

# 社交
GET/POST/PATCH/DELETE /api/v1/social/accounts[...]
GET/POST/PATCH/DELETE /api/v1/social/posts[...]
POST /api/v1/social/auto-distribute

# 社区
POST /api/v1/community/comments-draft
```

### 7.9 大屏 / 建议 / 报告

```
GET  /api/v1/dashboard/summary
GET  /api/v1/dashboard/rank-trends
GET  /api/v1/dashboard/social-funnel
GET  /api/v1/dashboard/backlink-stats
GET  /api/v1/dashboard/recommendations
POST /api/v1/dashboard/recommendations/generate
PATCH /api/v1/dashboard/recommendations/{id}
DELETE /api/v1/dashboard/recommendations/{id}
GET  /api/v1/reports/monthly
GET  /api/v1/reports/monthly/export
```

### 7.10 联调测试脚本

```bash
C:\Users\gcbc\anaconda3\envs\SEO\python.exe c:\SEO\backend\scripts\integration_test.py
```

自动验证 24 项 API 流程（需后端运行在 `127.0.0.1:8000`）。

---

## 八、运维与排错

### 8.1 常见问题

**Q：前端请求 401 未授权**

- Token 过期或未登录，重新登录
- 确认 `localStorage.access_token` 存在

**Q：前端请求 403「只读账号无权修改」**

- 当前账号角色为 `client_viewer`
- 需在数据库 `site_members` 表中将角色改为 `operator` 或 `admin`

**Q：SERP 排名不更新 / 显示 Mock**

- 确认 Celery Worker 正在运行
- 配置 `SCRAPINGBEE_API_KEY` 或 `PROXY_LIST`
- 页面顶部 SERP 模式应为 `scrapingbee` 或 `proxy`，而非 `mock`
- 手动触发：`POST /api/v1/serp/crawl/{keyword_id}`

**Q：AI 写作失败**

- 检查 `DEEPSEEK_API_KEY` 是否正确、有余额
- 查看后端控制台 DeepSeek 报错详情

**Q：GSC 连接失败**

- 检查 `GOOGLE_CLIENT_ID` / `GOOGLE_CLIENT_SECRET`
- 重定向 URI 必须与 Google Cloud 配置完全一致
- OAuth 回调后确认已选中正确的 GSC 属性

**Q：WordPress 同步失败**

- 确认默认站点的 `cms_type` 为 `wordpress`（见 [3.4](#34-cms-与-sitemap-配置可选)）
- API 凭证格式：`用户名:应用密码`（非登录密码）
- 目标站需启用 REST API，且无安全插件拦截

**Q：批量 SEO 审计只扫描 1 页**

- 检查 `sitemap_url` 是否可访问
- 部分站点 Sitemap 仅含首页或需要认证
- 可调大 `max_pages` 参数重试

**Q：Vite 代理 ECONNRESET**

- 后端未启动或 `--reload` 热重载卡住
- 重启 uvicorn：`python -m uvicorn app.main:app --host 127.0.0.1 --port 8000`

### 8.2 数据备份

```bash
# 备份 PostgreSQL
docker exec seo-postgres pg_dump -U seo seo_platform > backup_20260901.sql

# 恢复
docker exec -i seo-postgres psql -U seo seo_platform < backup_20260901.sql
```

### 8.3 日志位置

| 组件 | 日志 |
|------|------|
| 后端 API | uvicorn 控制台 |
| Celery Worker | worker 终端 |
| Celery Beat | beat 终端 |
| 前端 | `npm run dev` 终端 |

生产环境建议接入 ELK / Loki / Grafana 集中日志与监控。

### 8.4 服务检查命令

```bash
# Celery Worker 是否存活
celery -A app.core.celery_app inspect ping

# 数据库迁移版本
cd c:\SEO\backend && conda run -n SEO python -m alembic current
```

---

## 九、目录结构

```
c:\SEO\
├── backend/
│   ├── app/
│   │   ├── api/v1/          # REST 路由
│   │   │   ├── auth.py      # 认证
│   │   │   ├── keywords.py
│   │   │   ├── articles.py  # 文章 + 导入 + CMS 同步
│   │   │   ├── ai_writer.py
│   │   │   ├── seo_checker.py
│   │   │   ├── social.py
│   │   │   ├── community.py
│   │   │   ├── serp.py
│   │   │   ├── gsc.py
│   │   │   ├── backlinks.py
│   │   │   ├── competitors.py
│   │   │   ├── dashboard.py
│   │   │   └── reports.py   # 月度报告
│   │   ├── api/deps.py      # JWT + 默认站点解析 + 角色校验
│   │   ├── models/          # ORM（含 client_site, site_member, seo_audit）
│   │   ├── services/        # 业务逻辑（含 site_service.ensure_default_site）
│   │   ├── tasks/           # Celery 任务
│   │   └── main.py
│   ├── alembic/versions/    # 数据库迁移
│   ├── scripts/
│   │   └── integration_test.py  # API 联调脚本
│   ├── .env
│   └── pyproject.toml
├── frontend/
│   ├── src/
│   │   ├── api/             # HTTP 封装（含 reports.ts）
│   │   ├── views/           # 页面组件
│   │   │   ├── Dashboard.vue
│   │   │   ├── ArticleEditor.vue
│   │   │   ├── SeoChecker.vue
│   │   │   └── ...
│   │   ├── layout/DefaultLayout.vue
│   │   └── router/
│   └── vite.config.ts
├── docker-compose.yml
├── 使用手册.md              # 本文档
└── execution_plan.md
```

---

*文档版本：2026-09-02 · 单站模式，已移除客户站点管理界面*
