# SEO Platform 使用手册

> **SEO 运营平台**  
> 数据监控 · Reddit 社交分发 · 排名监控 · 运维看板

**文档版本**：2026-09-22

---

## 目录

1. [系统简介](#一系统简介)
2. [环境搭建](#二环境搭建)
3. [登录与权限](#三登录与权限)
4. [功能页面](#四功能页面)
5. [标准工作流](#五标准工作流)
6. [API 参考](#六api-参考)
7. [运维与排错](#七运维与排错)
8. [仓库结构](#八仓库结构)

---

## 一、系统简介

### 1.1 产品定位

SEO Platform 面向 **单站 / 多站 SEO 运营**：登录后自动关联默认工作空间；关键词、排名、Zernio Key、Reddit 账号与审核队列按站点隔离。可在顶栏切换当前站点（仅当前会话有效，刷新后回到默认站）。

当前能力：

| 能力 | 说明 |
|------|------|
| 排名监控 | 关键词库 + Google SERP 抓取（ScrapingBee / 代理 / Mock），TimescaleDB 存趋势 |
| Reddit 运营 | Zernio 接账号；DeepSeek 生成帖/评；审核后发布；矩阵、社区、品牌/产品词库 |
| 数据监控 | 关键词 Top10、Reddit 发帖/评论统计、排名趋势 |
| 运维看板 | Celery 队列积压、SERP 配额与成本估算、任务台账 |

### 1.2 页面与路由

| 页面 | 路由 | 说明 |
|------|------|------|
| 登录 | `/login` | 邮箱 + JWT；注册需 `ALLOW_REGISTRATION=true` |
| 数据监控 | `/dashboard` | 统计卡片与排名趋势 |
| 站点 | `/sites` | 客户站点 CRUD；顶栏切换当前站 |
| 社交分发 | `/social` | Reddit 创作、审核、发布（`RedditOperations`） |
| 社交账号 | `/accounts` | 当前站的 Zernio Key、同步 Reddit 账号 |
| 品牌 | `/brands` | 品牌 CRUD |
| 产品 | `/products` | 产品、搜索关键词、绑定社区 |
| 排名监控 | `/serp` | 关键词、抓取、趋势、快照 |
| 运维看板 | `/ops` | 队列 / 成本 / `task_runs` |

侧栏：**工作台**（数据监控、站点）· **运营与监控**（社交分发、社交账号、品牌、产品、排名监控、运维看板）。

未登录访问业务页 → `/login`；未知路径 → `/dashboard`。

### 1.3 架构

```
浏览器 Vue3 (:5173)  ── Authorization: Bearer ──►  FastAPI (:8000) /api/v1
                                                      ├── PostgreSQL + TimescaleDB
                                                      ├── Redis（业务 DB0 + Celery broker DB1）
                                                      └── Celery Worker / Beat
                                                              │
外部：ScrapingBee 或代理 · Zernio（Reddit）· DeepSeek（文案）
```

- 健康检查：`GET /api/health`（含 database / redis / celery_broker）
- 交互文档：`http://127.0.0.1:8000/docs`

### 1.4 站点隔离

- 首次调用业务 API 时自动创建 **默认站点**（`client_sites`）。
- 未传 `X-Site-Id` 时使用当前用户默认站点（按创建时间最早）。
- 前端顶栏可选择站点并注入 `X-Site-Id`；**仅内存会话有效**，刷新页面后回默认站。
- 站点管理页：`/sites`（名称、域名、状态、行业、备注）。成员 API 仍可用，暂无成员管理 UI。

---

## 二、环境搭建

### 2.1 前置要求

| 组件 | 要求 |
|------|------|
| Python | 3.11+（推荐 Conda 环境 `SEO`） |
| Node.js | 18+ |
| Docker | PostgreSQL + Redis |

### 2.2 数据库与 Redis

```bash
cd c:\SEO
docker compose up -d
```

- 镜像：`timescaledb:2.17.2-pg16`、`redis:7.4-alpine`
- 本地端口（`docker-compose.override.yml`）：`127.0.0.1:5432`、`127.0.0.1:6379`
- Redis 默认密码 `seo_dev_redis`，须与 `backend/.env` 中 `REDIS_PASSWORD`、`CELERY_BROKER_URL` 一致

生产（不映射 DB/Redis 端口、强制密码）：

```powershell
$env:POSTGRES_PASSWORD="强密码"
$env:REDIS_PASSWORD="强密码"
docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d
```

### 2.3 迁移与配置

```bash
cd c:\SEO\backend
conda run -n SEO python -m alembic upgrade head
```

复制 `backend/.env.example` → `backend/.env`，重点项：

```env
APP_ENV=dev
SECRET_KEY=随机强密钥
ALLOW_REGISTRATION=true          # 本地开发可开；生产默认 false
CORS_ORIGINS=                    # 生产填前端域名，逗号分隔

DATABASE_URL=postgresql+psycopg2://seo:seo_dev_pass@127.0.0.1:5432/seo_platform
REDIS_PASSWORD=seo_dev_redis
CELERY_BROKER_URL=redis://:seo_dev_redis@127.0.0.1:6379/1
CELERY_RESULT_BACKEND=redis://:seo_dev_redis@127.0.0.1:6379/2

SCRAPINGBEE_API_KEY=
SERP_ALLOW_PROXY_FALLBACK=false
SERP_DAILY_QUOTA=500
PROXY_LIST=

DEEPSEEK_API_KEY=sk-xxx
ZERNIO_API_BASE=https://zernio.com/api/v1
```

Zernio **API Key 只在前端「社交账号」添加**，不写 `.env`。

### 2.4 启动服务

**后端**

```bash
cd c:\SEO\backend
conda run -n SEO python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

**Celery Worker**（新终端）

```bash
conda run -n SEO celery -A app.core.celery_app worker --loglevel=info --pool=solo -Q default,serp,social
```

**Celery Beat**（新终端，定时抓取与 Reddit 自动化）

```bash
conda run -n SEO celery -A app.core.celery_app beat --loglevel=info
```

| Beat 任务 | 周期 | 说明 |
|-----------|------|------|
| `crawl-all-keywords` | 每 6h | 全量抓取 active 关键词 |
| `check-serp-failures` | 每日 09:30 | SERP 失败率 >30% 打日志告警 |
| `proxy-health-check` | 每 30min | 代理池检测 |
| `reddit-publish-scheduled` | 每 15min | 到期定时帖/评入队发布 |
| `reddit-recover-stale-posting` | 每 5min | 超时 `posting` → `failed` |
| `reddit-sync-post-metrics` | 每日 08:00 | 已发帖互动汇总 |
| `reddit-account-health` | 每日 09:00 | 账号健康与风控 |
| `reddit-smart-discover` | 每 3h | 智能发现讨论入待审 |

**前端**

```bash
cd c:\SEO\frontend
npm install
npm run dev
```

访问 `http://127.0.0.1:5173`；Vite 将 `/api` 代理到 `:8000`。

---

## 三、登录与权限

### 3.1 登录 / 注册

1. 打开 `/login`，副标题：**数据监控 · Reddit 社交分发 · 排名监控**
2. 登录：邮箱 + 密码（至少 6 位）
3. 注册：密码至少 8 位；若返回 403「未开放注册」，在 `.env` 设置 `ALLOW_REGISTRATION=true` 并重启后端

Token 写入 `localStorage.access_token`（默认 24h）。401 时前端清 Token 并跳转登录。

```http
POST /api/v1/auth/login
POST /api/v1/auth/register    # 需 ALLOW_REGISTRATION=true
GET  /api/v1/auth/me
Authorization: Bearer {token}
```

### 3.2 角色

| 角色 | 代码 | 说明 |
|------|------|------|
| 管理员 | `admin` | 新用户默认 |
| 运营 | `operator` | 读写业务数据 |
| 只读 | `client_viewer` | 写操作 403 |

站点成员 API：`/api/v1/sites/*`（成员邀请暂无前端；站点 CRUD 见 `/sites`）。

---

## 四、功能页面

### 4.0 站点（`/sites`）

- 增删改客户站点：名称、域名、状态、行业、备注  
- 顶栏下拉切换当前站；清空选择 = 默认站  
- 删除站点会级联清理该站关键词、社交账号、Zernio Key、Reddit 队列等数据  

### 4.1 数据监控（`/dashboard`）

| 卡片 | 口径 |
|------|------|
| 监控关键词数 | `active` 关键词数 |
| 关键词 Top10 占比 | 周期内成功快照且 rank≤10 占比 |
| Reddit 成功发帖 | 周期内 `reddit_posts.status=posted` |
| Reddit 评论发布 | 周期内 `reddit_comments.status=posted` |

页内说明：排名与社交指标 **不做自动因果归因**。时间范围 7 / 14 / 30 / 90 天；下方为关键词排名趋势图。

### 4.2 社交账号（`/accounts`）

1. 在 [zernio.com](https://zernio.com) 绑定 Reddit 并创建 API Key  
2. 本页为**当前站点**添加 Key → **同步账号** → 写入本站 `social_accounts`（Key 按站点隔离；顶栏切站后列表不同）  
3. 无启用 Key：同步失败；发布走 Mock，不会打真实 Reddit  
4. 历史全局 Key 迁移后归属于最早创建的站点；其它站需自行添加 

### 4.3 品牌 / 产品（`/brands`、`/products`）

- **品牌**：名称、启用状态  
- **产品**：归属品牌、产品关键词（供智能发现搜索）、绑定 Reddit 社区（约 10% 产品向评论用）  
- 编辑产品时品牌不可改；无品牌时产品页会提示先去品牌页  

### 4.4 社交分发（`/social`）

Reddit 工作台：选账号 → 发帖/评论审核 → 发布或定时。Key 与同步在 **社交账号** 页完成。

**发帖**：类型（踩坑 / 树洞 / 暴论 / 干货 / 求助）→ AI 生成 → 编辑 → 批准 → 发布或定时。  
状态：`pending_review` → `approved` → `posting` → `posted` | `failed`；失败可 **重试发布**；卡在 `posting` 可 **标记失败**。

**评论**：智能发现（按产品词 + 绑定社区）、粘贴 URL、关键词搜索批量生成 → 审核 → 发布。

**其它 Tab**：养号互动、账号矩阵（日限额 / Karma 阶段）、社区库。

混发约 **90% 日常 / 10% 产品向**；风控拦截（频次、外链、相似度等）返回 409。

### 4.5 排名监控（`/serp`）

- 添加关键词（词、落地页 URL、地区、优先级 1–5）→ 保存后触发首次抓取  
- Tab：**排名趋势** / **最新排名**（可触发抓取）/ **快照记录**（按 `success`、`blocked`、`timeout`、`error` 等筛）  
- 抓取优先级：ScrapingBee →（`SERP_ALLOW_PROXY_FALLBACK=true` 时）代理 → Mock  

### 4.6 运维看板（`/ops`）

| 区块 | 内容 |
|------|------|
| 队列 | `serp` / `social` / `default` 积压（读 Celery broker Redis） |
| 成本 | 当日 SERP 配额与估算费用、近 7 日 Reddit 发布成功率 |
| 任务台账 | 按 `business_type`、`business_id` 查最近 `task_runs` |

---

## 五、标准工作流

### 5.1 首次接入

```
登录 → 排名监控添加关键词并确认非 Mock → 社交账号添加 Zernio Key 并同步
→ 品牌/产品（可选，智能发现需要）→ 社交分发生成并审核发布
```

### 5.2 日常

```
排名监控看趋势 / 补抓 → 社交分发审核队列 → 数据监控看周期指标 → 运维看板看队列与任务
```

### 5.3 仅监控排名

启动 Worker + Beat，维护关键词库，用数据监控与 SERP 页观察即可，可不配置 Reddit。

---

## 六、API 参考

> 完整列表见 `http://127.0.0.1:8000/docs`  
> 业务前缀：`/api/v1`；健康检查：`GET /api/health`

| 分组 | 前缀 | 用途 |
|------|------|------|
| 认证 | `/auth` | register / login / me |
| 站点 | `/sites` | 站点 CRUD + 成员 API |
| 关键词 | `/keywords` | CRUD |
| 排名 | `/serp` | config / latest / snapshots / trends / crawl |
| 大屏 | `/dashboard` | summary / rank-trends |
| Reddit | `/reddit` | 账号、Key、帖评 CRUD、approve/publish/schedule/force-fail、discover、engage、品牌产品、社区 |
| 运维 | `/ops` | queues / costs / tasks |

Reddit 发布相关示例：

```
POST /api/v1/reddit/posts/{id}/approve|reject|publish|force-fail|schedule
POST /api/v1/reddit/comments/{id}/approve|reject|publish|force-fail|schedule
GET  /api/v1/ops/queues
GET  /api/v1/ops/costs
GET  /api/v1/ops/tasks?business_type=reddit_post&business_id=1
```

---

## 七、运维与排错

### 7.1 常见问题

| 现象 | 处理 |
|------|------|
| 401 | 重新登录；检查 `access_token` |
| 403 只读 | `site_members.role` 改为 `operator` / `admin` |
| 403 未开放注册 | `.env` 设 `ALLOW_REGISTRATION=true` |
| SERP 不更新 / Mock | Worker 监听 `serp`；配 ScrapingBee 或代理；手动「触发抓取」 |
| Reddit AI 502 | 检查 `DEEPSEEK_API_KEY` |
| 同步账号失败 | 社交账号页添加有效 Zernio Key |
| 未批准就发布 | 先批准；`failed` 可重试 |
| 一直「发布中」 | Worker `social` 队列 + Beat；**标记失败** 后重试；或 `force-fail` API |
| 队列深度恒为 0 | 运维 API 须读 `CELERY_BROKER_URL` 的 Redis DB（非 `REDIS_DB=0`） |
| 生产启动失败 | `APP_ENV=prod` 时需 `APP_DEBUG=false`、强 `SECRET_KEY` |

### 7.2 备份

```bash
docker exec seo-postgres pg_dump -U seo seo_platform > backup_YYYYMMDD.sql
docker exec -i seo-postgres psql -U seo seo_platform < backup_YYYYMMDD.sql
```

演练：恢复后 `alembic current` 正常、`/api/health` 可用、抽查关键词与 Reddit 数据。

### 7.3 检查命令

```bash
curl http://127.0.0.1:8000/api/health
cd c:\SEO\backend && conda run -n SEO celery -A app.core.celery_app inspect ping
cd c:\SEO\backend && conda run -n SEO python -m alembic current
```

### 7.4 生产环境变量

| 变量 | 说明 |
|------|------|
| `APP_ENV=prod` | 触发启动校验 |
| `APP_DEBUG=false` | 必填 |
| `SECRET_KEY` | ≥32 字符，非默认占位符 |
| `CORS_ORIGINS` | 前端域名；空则使用 `FRONTEND_URL` |
| `ALLOW_REGISTRATION=false` | 默认关闭自助注册 |
| `POSTGRES_PASSWORD` / `REDIS_PASSWORD` | 与 prod compose 一致 |

---

## 八、仓库结构

```
c:\SEO\
├── backend/
│   ├── app/api/v1/     auth · sites · keywords · serp · dashboard · reddit · ops
│   ├── app/services/   serp、reddit_publish、zernio_client、ai_writer …
│   ├── app/tasks/      serp_tasks · reddit_tasks · alert_tasks
│   ├── alembic/
│   ├── requirements.txt
│   └── .env
├── frontend/src/
│   ├── views/          Login · Dashboard · SocialScheduler · SocialAccounts
│   │                   Brands · Products · SerpMonitor · Ops
│   ├── components/     RedditOperations.vue
│   └── api/            http · keywords · serp · reddit · dashboard · ops
├── docker-compose.yml
├── docker-compose.override.yml
├── docker-compose.prod.yml
└── 使用手册.md
```

---

*SEO Platform · 数据监控 / Reddit 社交分发 / 排名监控 / 运维看板*
