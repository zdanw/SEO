# SEO Platform 使用手册

> **SEO 运营平台**  
> 关键词排名监控 · Reddit 创作发布 · 综合大屏

---

## 目录

1. [系统简介](#一系统简介)
2. [环境搭建](#二环境搭建)
3. [首次使用与账号体系](#三首次使用与账号体系)
4. [数据与权限](#四数据与权限)
5. [功能模块详解](#五功能模块详解)
6. [标准工作流](#六标准工作流)
7. [API 参考](#七api-参考)
8. [运维与排错](#八运维与排错)
9. [目录结构](#九目录结构)

---

## 一、系统简介

### 1.1 产品定位

SEO Platform 面向 **单站 SEO 运营**，登录后自动关联默认工作空间。关键词、排名快照、社交账号与发帖任务均按用户隔离，开箱即用，无需手动创建或切换客户站点。

当前版本聚焦三件事：**监控 Google 排名、Reddit 创作审核发布、在大屏看核心指标**。文章写作、SEO 检查、Search Console 页面、外链/竞品、优化工单、月度报告等模块已从产品中移除。其它社交平台预留 `SocialChannel` 接口，本期 UI 只做 Reddit。

### 1.2 核心能力

| 能力 | 说明 |
|------|------|
| 排名监控 | 维护关键词库，定时或手动抓取 Google SERP，TimescaleDB 存储趋势 |
| 社交分发 | 通过 Zernio 管理 Reddit 账号；AI 生成痛点答疑 / 经验 / 对比帖与评论；强制审核后发布；支持账号矩阵、社区库、风控与定时发布 |
| 综合大屏 | 关键词 Top10 占比、发帖数、引流点击、排名趋势与社交漏斗 |

### 1.3 功能模块与路由

| 模块 | 页面路由 | 说明 |
|------|----------|------|
| 登录 / 注册 | `/login` | 邮箱账号登录与注册 |
| 综合大屏 | `/dashboard` | 统计卡片、排名趋势、社交漏斗 |
| 社交分发 | `/social` | Reddit 账号、发帖审核、评论审核 |
| 排名监控 | `/serp` | 关键词、最新排名、趋势、抓取快照 |

未登录访问其它路由会跳转 `/login`；未知路径回退到 `/dashboard`。

### 1.4 技术架构

```
浏览器 (Vue3 :5173)
    │  Header: Authorization: Bearer {token}
    ▼
FastAPI (:8000)  /api/v1
    ├── PostgreSQL + TimescaleDB（业务数据 + 排名时序）
    ├── Redis（Celery Broker）
    └── Celery Worker / Beat（SERP 抓取、Reddit 定时发布、失败率告警）
            │
            ▼
外部服务：ScrapingBee 或代理池 / Zernio（Reddit） / DeepSeek（Reddit 文案生成）
```

健康检查：`GET /api/health`  
交互式文档：`http://127.0.0.1:8000/docs`

### 1.5 数据隔离原则

- 首次访问业务 API 时，后端自动为用户创建或关联 **默认站点**（`client_sites` 表，名称「默认站点」，域名 `localhost`）。
- 前端 **无需** 传递 `X-Site-Id`；未传时后端使用当前用户的默认站点。
- 关键词落地页、发帖外链可填写任意有效 URL。

---

## 二、环境搭建

### 2.1 前置要求

| 组件 | 版本要求 |
|------|----------|
| Python | 3.11+（推荐 Conda 环境名 `SEO`） |
| Node.js | 18+ |
| Docker | 用于 PostgreSQL + Redis |
| Conda | 管理 Python 依赖 |

### 2.2 启动数据库

```bash
cd c:\SEO
docker compose up -d
# 等待 PostgreSQL 和 Redis 就绪（约 30 秒）
```

容器：`seo-postgres`（TimescaleDB / PostgreSQL 16）、`seo-redis`。

### 2.3 初始化数据库

```bash
cd c:\SEO\backend
conda run -n SEO python -m alembic upgrade head
```

迁移会创建当前产品所需表：`users`、`client_sites`、`site_members`、`keywords`、`serp_rank_snapshots`、`social_accounts`、`social_posts`、`zernio_api_keys`、`reddit_posts`、`reddit_comments`、账号档案 / 社区库 / 词库 / 帖子指标等。  
首次调用业务 API 时，系统会自动为当前用户创建「默认站点」。

### 2.4 配置环境变量

复制并编辑 `backend/.env`（参考 `backend/.env.example`）。当前产品真正用到的项：

```env
# 应用
SECRET_KEY=请替换为随机强密钥
FRONTEND_URL=http://127.0.0.1:5173
API_V1_PREFIX=/api/v1

# 数据库 / Redis（与 docker-compose 默认值一致时可沿用）
DATABASE_URL=postgresql+psycopg2://seo:seo_dev_pass@127.0.0.1:5432/seo_platform
CELERY_BROKER_URL=redis://127.0.0.1:6379/1
CELERY_RESULT_BACKEND=redis://127.0.0.1:6379/2

# SERP 抓取（二选一或同时配置，优先 ScrapingBee）
SCRAPINGBEE_API_KEY=xxxxxxxx
PROXY_LIST=http://user:pass@proxy:port

# DeepSeek（Reddit AI 生成必填；未配置则 generate 失败，不入库）
DEEPSEEK_API_KEY=sk-xxxxxxxx
DEEPSEEK_MODEL=deepseek-chat

# Zernio API 端点（API Key 只在前端「社交账号」配置，不写 .env）
ZERNIO_API_BASE=https://zernio.com/api/v1
ZERNIO_TIMEOUT=30

# 邮件告警（可选，SERP 失败率过高时日志/邮件）
SMTP_HOST=
ALERT_EMAIL_TO=
```

Reddit 账号在 [zernio.com](https://zernio.com) 绑定；API Key 统一在前端 **社交账号** 页面添加后同步。

### 2.5 启动后端

```bash
cd c:\SEO\backend
conda run -n SEO python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

- Swagger 文档：`http://127.0.0.1:8000/docs`
- 健康检查：`http://127.0.0.1:8000/api/health` 返回 `"status": "ok"` 即表示后端正常

### 2.6 启动 Celery Worker（新终端）

```bash
cd c:\SEO\backend
conda run -n SEO celery -A app.core.celery_app worker --loglevel=info --pool=solo -Q default,serp,social
```

负责：SERP 排名抓取、社交发帖、Reddit 定时发布、代理池健康检测。

### 2.7 启动 Celery Beat（新终端，可选）

```bash
cd c:\SEO\backend
conda run -n SEO celery -A app.core.celery_app beat --loglevel=info
```

定时任务：

| 任务 | 周期 | 说明 |
|------|------|------|
| `crawl-all-keywords` | 每 6 小时 | 全量抓取 `active` 关键词 |
| `check-serp-failures` | 每日 09:30 | 统计近 24h 抓取失败率，超过 30% 打告警日志 |
| `proxy-health-check` | 每 30 分钟 | 代理池健康检测 |
| `reddit-publish-scheduled` | 每 15 分钟 | 发布到期的 Reddit 定时帖/评论 |
| `reddit-sync-post-metrics` | 每日 08:00 | 汇总已发帖互动数据 |
| `reddit-account-health` | 每日 09:00 | 账号健康检查与风控预警 |

### 2.8 启动前端

```bash
cd c:\SEO\frontend
npm install
npm run dev
```

访问 `http://127.0.0.1:5173`，未登录将跳转登录页。Vite 将 `/api` 代理到 `http://127.0.0.1:8000`。

---

## 三、首次使用与账号体系

### 3.1 注册与登录

**方式一：前端页面**

1. 打开 `http://127.0.0.1:5173/login`
2. 「注册新账号」：填写邮箱（必填）、昵称（选填）、密码（至少 8 位）
3. 注册成功后切换「登录」Tab 登录（登录密码至少 6 位）

登录页副标题为「综合大屏 · 社交分发 · 排名监控」。登录成功进入 `/dashboard`。

**方式二：API**

```http
POST /api/v1/auth/register
Content-Type: application/json

{
  "email": "admin@example.com",
  "password": "your_password_8+",
  "full_name": "运营管理员"
}
```

```http
POST /api/v1/auth/login
Content-Type: application/json

{
  "email": "admin@example.com",
  "password": "your_password_8+"
}
```

响应示例：

```json
{
  "access_token": "eyJhbGci...",
  "token_type": "bearer",
  "expires_in": 86400
}
```

Token 默认有效期 24 小时（`ACCESS_TOKEN_EXPIRE_MINUTES=1440`）。

```http
GET /api/v1/auth/me
Authorization: Bearer {access_token}
```

### 3.2 Token 存储

登录成功后前端自动写入：

| Key | 说明 |
|-----|------|
| `localStorage.access_token` | JWT，后续请求 `Authorization` 头 |
| `localStorage.user_email` | 顶栏展示用邮箱 |

401 时前端会清除 Token 并跳转登录页。

### 3.3 登录后即用

登录成功后即可使用三个功能页，**无需** 手动创建或选择客户站点：

1. 首次访问业务 API 时，后端自动创建名为「默认站点」的工作空间
2. 关键词、排名、社交账号与发帖任务自动归属该站点
3. 建议先到 **排名监控** 添加关键词并触发抓取，再到 **社交账号** 添加 Zernio Key 并同步 Reddit 账号

### 3.4 站点 API（可选，无独立页面）

站点列表、创建、成员管理仍可通过 `/api/v1/sites` 调用（见 [7.2](#72-客户站点)）。前端当前不提供站点管理界面，默认始终使用自动创建的默认站点。

---

## 四、数据与权限

### 4.1 内部站点模型

系统用 `client_sites` 做数据隔离，对日常用户透明：

| 字段 | 说明 |
|------|------|
| `name` | 站点显示名称（默认「默认站点」） |
| `domain` | 主域名（默认 `localhost`） |
| `status` | `active` / `paused` / `archived` |
| `industry` | 选填 |

表中仍保留 `cms_type`、`cms_api_url`、`sitemap_url` 等历史字段，当前版本无对应产品功能。

### 4.2 成员角色（内部）

| 角色 | 代码 | 权限 |
|------|------|------|
| 管理员 | `admin` | 全部读写（新用户默认） |
| 运营专员 | `operator` | 关键词 / 社交 / 抓取等写操作 |
| 只读 | `client_viewer` | 仅查看，不可修改 |

新注册用户自动获得默认站点的 `admin` 角色。只读账号写操作返回 403「只读账号无权修改数据」。

### 4.3 API 请求头规范

业务请求仅需携带：

```http
Authorization: Bearer {access_token}
```

`X-Site-Id` 为可选项；未传时后端自动使用当前用户的默认站点。

---

## 五、功能模块详解

### 5.1 综合大屏（`/dashboard`）

**用途**：关键词与社交核心指标一览。

**统计卡片（4 张）**

| 卡片 | 含义 |
|------|------|
| 监控关键词数 | 状态为 `active` 的关键词数 |
| 关键词 Top10 占比 | 周期内有排名快照且位次 ≤ 10 的比例 |
| 成功发帖数 | 周期内 `posted` 帖数，带较上周期环比 |
| 社交引流点击 | 已发帖 `engagement.clicks` 合计 |

**图表**

- **关键词排名趋势**：近 N 天日均排名曲线（Y 轴越小越好）。无数据时显示「暂无数据」；仅 1 次抓取时提示多次抓取后可看趋势。
- **社交分发漏斗**：创建任务 → 成功发帖 → 引流点击。无数据时提示先到「社交分发」创建任务。

**工具栏**

| 操作 | 说明 |
|------|------|
| 时间范围 | 7 / 14 / 30 / 90 天 |
| 刷新 | 重新拉取卡片与图表 |

**API**

```
GET  /api/v1/dashboard/summary?days=7
GET  /api/v1/dashboard/rank-trends?days=14
GET  /api/v1/dashboard/social-funnel?days=7
```

`rank-trends` 可传 `keyword_ids=1,2`；不传则取周期内最近有成功快照的最多 5 个词。

---

### 5.2 社交账号（`/accounts`）

1. 在 [zernio.com](https://zernio.com) 绑定 Reddit 账号并创建 API Key。
2. 本页「Zernio Key」→ 添加 Key（可多把；免费档每把大约 2 个 Reddit 号；可选填 Profile ID）。
3. 点「同步账号」，从 Zernio 拉取已绑定的 `u/xxx` 写入 `social_accounts`。
4. 未在页面配置任何启用 Key：同步会失败并提示添加 Key；无 Key 的发布走 Mock，不会打到真实 Reddit。
5. 「在 Zernio 管理账号」打开 Zernio 控制台；绑定新号后回到本页再点同步。

---

### 5.3 社交分发（`/social`）

**用途**：Reddit 创作与发布工作台。Reddit 账号经 Zernio 接入；其它平台预留 `SocialChannel` 协议，本期页面只做 Reddit。

Zernio Key 与账号同步请到 **社交账号**（`/accounts`）管理；本页仅选择当前发布账号。

#### 5.3.1 发帖审核

1. 选类型：痛点答疑（`consultation`）、经验分享（`experience`，可选站点软链）或对比测评（`comparison`）
2. 填子版块（不含 `r/`）和主题关键词 →「AI 生成并入审核队列」
3. 队列中可编辑标题/正文/子版块 → **批准** → **发布**，或批准后设置定时发布时间
4. 未批准直接发布会 400。失败记录可「重试发布」，不必重新批准。风控拦截（频次/外链去重/内容相似度等）返回 409。

状态：`pending_review` → `approved` → `posting` → `posted` | `failed`；未发布可 `rejected`。

#### 5.3.2 评论审核

- **粘贴 URL**：Reddit 帖链接 → AI 按帖子上下文生成评论 → 审核 → 发布
- **关键词搜索**：子版块 + 关键词 → 勾选结果 → 批量生成 → 审核 → 发布

#### 5.3.3 账号矩阵 / 社区 / 词库

- **账号档案**：养号号 / 种草号 / 答疑号，带每日发帖/评论限额与 Karma 阶段
- **社区库**：核心 / 长尾子版块、是否允许外链、自推广星期
- **双词库**：SEO 适配词 + AI 热搜词；可一键灌入方案预设

#### 5.3.4 API

```
GET    /api/v1/reddit/status
POST   /api/v1/reddit/accounts/sync
GET    /api/v1/reddit/zernio-keys
POST   /api/v1/reddit/zernio-keys
PATCH  /api/v1/reddit/zernio-keys/{id}
DELETE /api/v1/reddit/zernio-keys/{id}
GET    /api/v1/reddit/oauth/start          # Zernio 托管授权 URL（无 Key 时 Mock）
GET    /api/v1/reddit/overview
POST   /api/v1/reddit/posts/generate
GET    /api/v1/reddit/posts
PATCH  /api/v1/reddit/posts/{id}
POST   /api/v1/reddit/posts/{id}/approve|reject|publish
POST   /api/v1/reddit/posts/{id}/schedule
POST   /api/v1/reddit/comments/generate
POST   /api/v1/reddit/comments/generate-batch
GET    /api/v1/reddit/comments
PATCH  /api/v1/reddit/comments/{id}
POST   /api/v1/reddit/comments/{id}/approve|reject|publish
GET    /api/v1/reddit/discover/search?subreddit=&keyword=
GET    /api/v1/reddit/accounts/profiles
PATCH  /api/v1/reddit/accounts/{id}/profile
GET    /api/v1/reddit/communities
POST   /api/v1/reddit/communities/seed
GET    /api/v1/reddit/keywords
```

账号同步与发布均走 `/api/v1/reddit/*`。

---

### 5.4 排名监控（`/serp`）

**用途**：跟踪目标关键词在 Google 的排名变化。关键词在本页添加，无独立「关键词库」菜单。

#### 5.3.1 添加关键词

右上角「添加关键词」，保存后自动触发首次抓取。

| 字段 | 说明 |
|------|------|
| 关键词 | 如 `best python seo tools` |
| 落地页 URL | 选填，可填写任意 URL |
| 搜索地区 | `global` / `us`（默认）/ `uk` / `cn` / `hk` |
| 优先级 | 1（最高）– 5（最低），默认 3 |
| 搜索引擎 | 接口默认 `google`（页面不单独选择） |

#### 5.3.2 三个 Tab

| Tab | 内容 |
|-----|------|
| 排名趋势 | 多选关键词、7 / 30 / 90 天曲线；顶部显示抓取模式 |
| 最新排名 | 各 `active` 词当前位次、抓取状态、最后抓取时间；可「触发抓取」或删除 |
| 快照记录 | 历史 `serp_rank_snapshots`，可按 `success` / `blocked` / `timeout` / `error` 筛选 |

删除关键词会同时删除其排名快照。

#### 5.3.3 抓取机制

- **定时**：Celery Beat 约每 6 小时全量抓取
- **手动**：最新排名表「触发抓取」，或 `POST /api/v1/serp/crawl/{keyword_id}`
- **存储**：`serp_rank_snapshots` 时序表（复合主键 `time + keyword_id`）
- 趋势接口按 6 小时 `time_bucket` 聚合平均排名

#### 5.3.4 代理配置优先级

1. `SCRAPINGBEE_API_KEY` 已配置 → ScrapingBee Google API（页面提示「排名数据来自 Google 实时抓取」）
2. `PROXY_LIST` / `PROXY_ENDPOINT` 已配置 → 住宅代理直连 Google
3. 均未配置 → Mock 模式（随机排名，仅供开发；页面黄色告警）

**API**

```
GET    /api/v1/keywords
POST   /api/v1/keywords
DELETE /api/v1/keywords/{id}
GET    /api/v1/serp/config
GET    /api/v1/serp/latest
GET    /api/v1/serp/snapshots?keyword_id=&crawl_status=&page=&size=
GET    /api/v1/serp/trends?keyword_ids=1,2&days=30
POST   /api/v1/serp/crawl/{keyword_id}
```

---

## 六、标准工作流

### 6.1 首次接入（约 10 分钟）

```
1. 注册登录 → 系统自动创建默认工作空间
2. 排名监控 → 添加核心关键词 → 确认抓取模式非 mock（生产环境）
3. 等待首次抓取完成（约 10 秒后刷新「最新排名」）
4. 社交账号 → 添加 Zernio Key → 同步 Reddit 账号 → 社交分发生成帖子进入审核队列
```

### 6.2 日常运营

```
1. 排名监控 → 查看趋势 / 触发补抓 / 增删关键词
2. 社交分发 → AI 生成帖/评论 → 编辑批准 → 发布
3. 综合大屏 → 按 7/14/30/90 天查看 Top10 占比与漏斗
```

### 6.3 纯监控型（不发社交）

```
1. 添加并抓取关键词
2. 启动 Celery Worker + Beat，保持 6 小时周期抓取
3. 大屏与「排名趋势」观察位次变化
```

---

## 七、API 参考

> 完整交互式文档：`http://127.0.0.1:8000/docs`  
> 前缀：`/api/v1`（健康检查除外：`GET /api/health`）

### 7.1 认证

```
POST /api/v1/auth/register     { email, password, full_name? }
POST /api/v1/auth/login        { email, password } → { access_token, token_type, expires_in }
GET  /api/v1/auth/me           当前用户信息
```

### 7.2 客户站点

```
GET    /api/v1/sites
POST   /api/v1/sites
GET    /api/v1/sites/{site_id}
PATCH  /api/v1/sites/{site_id}
DELETE /api/v1/sites/{site_id}
GET    /api/v1/sites/{site_id}/members
POST   /api/v1/sites/{site_id}/members
```

前端不暴露这些页面；删除站点会级联清理该站关键词、快照、社交账号与发帖。

### 7.3 关键词

```
GET    /api/v1/keywords
POST   /api/v1/keywords          { keyword, target_url?, search_engine?, region?, priority? }
DELETE /api/v1/keywords/{id}     同时删除该词快照
```

### 7.4 排名监控

```
GET  /api/v1/serp/config
GET  /api/v1/serp/latest
GET  /api/v1/serp/snapshots
GET  /api/v1/serp/trends
POST /api/v1/serp/crawl/{keyword_id}
```

### 7.5 Reddit 运营

```
GET    /api/v1/reddit/status
POST   /api/v1/reddit/accounts/sync
GET    /api/v1/reddit/zernio-keys
POST   /api/v1/reddit/posts/generate
GET    /api/v1/reddit/posts
POST   /api/v1/reddit/posts/{id}/approve|reject|publish
POST   /api/v1/reddit/posts/{id}/schedule
POST   /api/v1/reddit/comments/generate
POST   /api/v1/reddit/comments/generate-batch
GET    /api/v1/reddit/comments
POST   /api/v1/reddit/comments/{id}/approve|reject|publish
GET    /api/v1/reddit/discover/search
GET    /api/v1/reddit/overview
GET    /api/v1/reddit/communities
GET    /api/v1/reddit/keywords
```

### 7.6 数据大屏

```
GET  /api/v1/dashboard/summary
GET  /api/v1/dashboard/rank-trends
GET  /api/v1/dashboard/social-funnel
```

### 7.7 已移除的接口（勿再调用）

以下路径在当前后端 **未注册**，调用会 404：

- `/api/v1/articles`、`/api/v1/ai/*`
- `/api/v1/seo/*`
- `/api/v1/gsc/*`
- `/api/v1/backlinks`、`/api/v1/competitors`
- `/api/v1/community/*`
- `/api/v1/reports/*`、`/api/v1/dashboard/recommendations*`、`/api/v1/dashboard/backlink-stats`

---

## 八、运维与排错

### 8.1 常见问题

**Q：前端请求 401 未授权**

- Token 过期或未登录，重新登录
- 确认 `localStorage.access_token` 存在

**Q：前端请求 403「只读账号无权修改」**

- 当前账号角色为 `client_viewer`
- 在数据库 `site_members` 表中将角色改为 `operator` 或 `admin`

**Q：SERP 排名不更新 / 显示 Mock**

- 确认 Celery Worker 正在运行，且监听了 `serp` 队列
- 配置 `SCRAPINGBEE_API_KEY` 或 `PROXY_LIST`
- 页面顶部模式应为 `scrapingbee` 或 `proxy`，而非 `mock`
- 手动触发：排名监控 →「触发抓取」，或 `POST /api/v1/serp/crawl/{keyword_id}`

**Q：Reddit AI 生成失败**

- 检查 `DEEPSEEK_API_KEY` 是否有效、有余额
- 未配置时接口返回 502，不会写入审核队列

**Q：同步 Reddit 账号失败 / oauth/start 503**

- 先到 **社交账号** 页面添加并启用 Zernio API Key（不再使用 `.env`）
- 确认 Key 在 Zernio 控制台有效，且已绑定 Reddit 账号
- 未配 Key 时同步会失败；无 Key 的发布走 Mock，不会打到真实 Reddit

**Q：未批准就点发布**

- 接口返回 400「请先批准后再发布」。先点批准（或对失败记录点重试发布）

**Q：大屏排名趋势为空**

- 先添加关键词并完成至少一次成功抓取
- 确认快照 `crawl_status=success` 且 `rank` 非空

**Q：Vite 代理 ECONNRESET**

- 后端未启动或 `--reload` 热重载卡住
- 重启 uvicorn：`python -m uvicorn app.main:app --host 127.0.0.1 --port 8000`

### 8.2 数据备份

```bash
# 备份 PostgreSQL
docker exec seo-postgres pg_dump -U seo seo_platform > backup_20260917.sql

# 恢复
docker exec -i seo-postgres psql -U seo seo_platform < backup_20260917.sql
```

### 8.3 日志位置

| 组件 | 日志 |
|------|------|
| 后端 API | uvicorn 控制台 |
| Celery Worker | worker 终端 |
| Celery Beat | beat 终端 |
| 前端 | `npm run dev` 终端 |

生产环境建议接入 ELK / Loki / Grafana 集中日志与监控。

### 8.4 服务检查命令

```bash
# 健康检查
curl http://127.0.0.1:8000/api/health

# Celery Worker 是否存活
celery -A app.core.celery_app inspect ping

# 数据库迁移版本
cd c:\SEO\backend && conda run -n SEO python -m alembic current
```

---

## 九、目录结构

```
c:\SEO\
├── backend/
│   ├── app/
│   │   ├── api/v1/          # REST 路由
│   │   │   ├── auth.py      # 认证
│   │   │   ├── sites.py     # 站点（API，无独立前端页）
│   │   │   ├── keywords.py  # 关键词库
│   │   │   ├── reddit.py    # Zernio Key / 账号同步 / 审核发布
│   │   │   ├── serp.py      # 排名查询与触发抓取
│   │   │   └── dashboard.py # 大屏汇总
│   │   ├── api/deps.py      # JWT + 默认站点解析 + 角色校验
│   │   ├── models/          # User / ClientSite / Keyword / Serp / Social / Reddit / Zernio
│   │   ├── services/        # 抓取、zernio_client、reddit_publish、reddit_risk
│   │   ├── tasks/           # serp_tasks / alert_tasks / reddit_tasks
│   │   └── main.py
│   ├── alembic/versions/    # 数据库迁移
│   ├── .env
│   └── pyproject.toml
├── frontend/
│   ├── src/
│   │   ├── api/             # http / keywords / serp / reddit / dashboard
│   │   ├── views/
│   │   │   ├── Login.vue
│   │   │   ├── Dashboard.vue
│   │   │   ├── SocialScheduler.vue
│   │   │   ├── SocialAccounts.vue
│   │   │   ├── BrandProducts.vue
│   │   │   └── SerpMonitor.vue
│   │   ├── components/RedditOperations.vue
│   │   ├── layout/DefaultLayout.vue
│   │   └── router/index.ts
│   └── vite.config.ts
├── docker-compose.yml
└── 使用手册.md              # 本文档
```

侧栏菜单：工作台（综合大屏）· 运营与监控（社交分发、排名监控）。

---

*文档版本：2026-09-17 · 当前产品范围：综合大屏 / Reddit 社交分发 / 排名监控*
