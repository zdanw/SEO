P2：N9 过度修复——反向注入失去具体示例
python
# 当前：为防串站完全不写 matches
increments[key] = ...        # matches 一律丢弃
return [(k, []) for k, _ in ranked[:limit]]   # samples 永远为空
矛盾点：你已经按 site_id 分桶了（reddit:ai_patterns:v1:{site_id}），跨站路径本就堵死，却把本站点最有价值的具体示例一起砍了。_match_samples 已成死代码。

修法：写入前对品牌词脱敏，保留 matches：

python
def _redact(text: str, brand_names: set[str]) -> str:
    for b in brand_names:
        text = re.sub(re.escape(b), "[brand]", text, flags=re.I)
    return text
🟠 P3：site_id 默认 None 静默失效
python
def record_detection_patterns(signs_of_ai, *, site_id: int | None) -> None:
    if site_id is None or not signs_of_ai:
        return                    # 静默 return
漏传时整条链路静默关闭，不报错不打日志。建议去掉默认值（改必填）或加 logger.warning。

🟠 P4：find_similar_on_site 仍用字符级查重
difflib.SequenceMatcher 抓不到"换词不换意"

limit(60) 会漏掉 7 天内超过 60 条的情况

返回 str | None 丢失相似度/对方 ID，UI 无法做"跳转查看"

🟡 P5：brand_leak 路径返回半成品
两轮都撞品牌名时，返回的是未经 humanize 的原始 AI 输出，ai_score_before/after_humanize 均为 None——所有分支里最"AI 味"的版本反而直接进审核。

🟡 P6：_mem_examples 只写不清
读取时判 TTL ✅，但过期条目永不删除，长期运行缓慢膨胀。建议读取时 pop 过期项。