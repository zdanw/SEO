# SEO Platform 精简改进清单

> 适用范围：当前产品（排名监控 · Reddit 分发 · 综合大屏）  
> 原则：不换技术栈、不大搬家目录、不做微服务拆分；按痛点渐进补强。  
> 对照日期：2026-09-21

旧版「全功能 SEO 平台架构评审」已过时，本文档取代之。

---

## 优先级总览

| 优先级 | 主题 | 目标 |
|--------|------|------|
| P0 | 部署安全与环境隔离 | 开发可跑、生产不裸奔 |
| P1 | Reddit 发布可靠性 | 幂等、可重试、失败可恢复 |
| P2 | SERP 数据质量与成本 | 可追溯、可切换供应商、可控预算 |
| P3 | 可观测与分析深化 | 看得见故障与成本，指标可信 |

---

## P0：部署安全与环境隔离

**现状**：`docker-compose.yml` 中 Redis 空密码、Postgres/Redis 端口直映射主机；镜像使用 `latest-pg16`。本地开发可接受，直接上公网有风险。

| # | 项 | 做法 | 验收 |
|---|----|------|------|
| 0.1 | Redis 认证 | 去掉空 `--requirepass ""`，用环境变量注入密码；Broker URL 同步带密码 | 无密码无法连接 |
| 0.2 | 端口暴露 | 生产用 Docker 内部网络；仅 API/前端对外；或绑定 `127.0.0.1` | 外网扫不到 5432/6379 |
| 0.3 | 固定镜像版本 | Postgres/Redis 钉死具体 tag（必要时 digest） | compose 无 `latest` 漂浮依赖 |
| 0.4 | 环境分层 | 明确 `dev` / `prod`：密钥、CORS、DEBUG、备份策略分文件或分 profile | 生产 `.env` 不含开发默认口令 |
| 0.5 | 备份可恢复 | 文档化 `pg_dump` 周期；至少做一次恢复演练 | 能从备份还原到可用库 |

**不做**：为本地开发强行上 Vault/K8s；现阶段 compose + 环境变量即可。

---

## P1：Reddit 发布可靠性

**现状**：帖/评已有 `draft → approved → posting → posted/failed`；`posted` 会跳过重复发布；Celery 已分 `social` 队列。仍需补强并发与失败恢复。

| # | 项 | 做法 | 验收 |
|---|----|------|------|
| 1.1 | 并发幂等 | 发布前用 DB 条件更新（仅 `approved`/`failed` → `posting`）或行锁，避免双 Worker 重复发 | 同一条记录并发触发只成功发一次 |
| 1.2 | 外部结果回写 | 已有 `reddit_post_id` / `reddit_comment_id` 时，重试只做状态修复，不再调发布 API | 网络超时后重试不产生双帖 |
| 1.3 | 失败可操作 | `failed` 保留正文与错误信息；前端「重试发布」走同一幂等路径 | 审核内容不因失败丢失 |
| 1.4 | 定时任务去重 | Beat 扫到期内容时，按记录 ID 去重入队，避免积压重复任务 | 队列中同 ID 不堆多条 |
| 1.5 | 超时与熔断 | 发布调用设明确 timeout；沿用/收紧 Zernio 限流与熔断 | 卡住任务会被软超时杀掉并记失败 |

**不做**：Transactional Outbox、独立发布微服务。

---

## P2：SERP 数据质量与成本

**现状**：`SerpCrawler` + ScrapingBee/代理；快照有 `crawl_status`、`rank` 可空；Celery 有 `serp` 队列与失败率告警。缺统一 Provider 契约与成本账。

| # | 项 | 做法 | 验收 |
|---|----|------|------|
| 2.1 | Provider 接口 | 抽出 `SERPProvider`（入参：关键词/地区/语言/设备；出参：rank、url、status、raw 元数据） | 切换 ScrapingBee / 其它 API 不改业务任务代码 |
| 2.2 | 状态语义固定 | 统一：`success` / `not_found` / `blocked` / `timeout` / `error`；`rank=NULL` 必须带 status，禁止当成「第 100 名」 | 大屏与趋势只统计 `success` |
| 2.3 | 采集元数据 | 快照或旁表记录 provider、配额消耗、耗时；保留必要 raw（可截断） | 能回答「这次数据从哪来、花了多少」 |
| 2.4 | 频率与预算 | 按关键词优先级调 Beat 频率；日/月配额上限；超限告警或降频 | 账单可预期，高峰不打爆配额 |
| 2.5 | 合规优先 | 默认走合规 SERP API；代理直抓仅作降级/实验，并在配置中显式开关 | 文档写清默认路径与风险 |

**不做**：自建大规模住宅代理池当主路径；不必为此再引入 Timescale 之外的时序库。

---

## P3：可观测与分析深化

**现状**：有 SERP 失败率定时检查、Reddit 指标同步与账号健康检查；缺统一任务台账与成本面板。

| # | 项 | 做法 | 验收 |
|---|----|------|------|
| 3.1 | 任务可观测 | 关键任务记录关键字段：任务名、关联业务 ID、开始/结束、结果、错误、重试次数（表或结构化日志均可） | 能按帖子/关键词查最近一次任务结果 |
| 3.2 | 队列健康 | 暴露或脚本查看 `serp` / `social` / `default` 积压长度；积压超阈值告警 | 堵塞能在小时内发现 |
| 3.3 | 成本指标 | 汇总：SERP 调用次数与费用估算、LLM Token/篇成本、发布成功率 | 大屏或运维页能看到周趋势 |
| 3.4 | 关联 ID | API → Celery 传递 `request_id` / `site_id`，日志带上 | 一条失败链路能串起来 |
| 3.5 | 分析可信 | 大屏指标注明时间范围与样本口径；排名变化与 Reddit 互动不做自动因果结论 | UI/文案无「因发帖导致排名上升」类断言 |

**不做**：上完整 ELK/APM 套件作为前置条件；先结构化日志 + 轻量统计表即可。

---

## 明确不做（避免过度架构）

- 按「内容 / SEO / 发布 / 监控 / 分析」五大模块重做目录树
- 文章 CMS 生命周期（Draft → Review → Published）——当前主路径是 Reddit，不是站内发文
- 微服务拆分、消息总线、OLAP
- 为「实时」再叠一层时序中间件（已有 TimescaleDB 够用）
- 外链、竞品、站内 SEO 检查等已下线能力回潮

---

## 建议实施顺序（约 2–4 周可穿插完成）

```
第 1 周   P0 全部 + P1.1 / P1.2
第 2 周   P1 收尾 + P2.1 / P2.2
第 3 周   P2.3–P2.5 + P3.1 / P3.2
第 4 周   P3.3–P3.5（按业务需要取舍）
```

每完成一项，在下表勾选并注明 PR / 日期。

| 完成 | ID | 备注 |
|------|-----|------|
| ☑ | 0.1 | Redis 密码 + Celery URL |
| ☑ | 0.2 | 本机绑定；prod 覆盖去掉端口 |
| ☑ | 0.3 | timescaledb:2.17.2-pg16 / redis:7.4-alpine |
| ☑ | 0.4 | docker-compose.prod.yml + 手册说明 |
| ☑ | 0.5 | 手册补充备份恢复演练清单 |
| ☑ | 1.1 | 条件更新抢占 posting |
| ☑ | 1.2 | 已有外部 ID 只修复状态 |
| ☑ | 1.3 | 前端重试发布 + 失败原因列；failed 可编辑 |
| ☑ | 1.4 | Redis NX 按 ID 去重入队 |
| ☑ | 1.5 | Zernio 超时 504；任务软超时；熔断收紧 |
| ☑ | 2.1 | SERPProvider + get_serp_provider |
| ☑ | 2.2 | success/not_found/blocked/timeout/error |
| ☑ | 2.3 | serp_features 含 provider/duration/cost |
| ☑ | 2.4 | SERP_DAILY_QUOTA + 优先级降频 |
| ☑ | 2.5 | SERP_ALLOW_PROXY_FALLBACK 默认关闭 |
| ☑ | 3.1 | task_runs 表 + 发布/抓取写入 |
| ☑ | 3.2 | GET /ops/queues |
| ☑ | 3.3 | GET /ops/costs |
| ☑ | 3.4 | X-Request-ID 中间件传入 Celery |
| ☑ | 3.5 | 大屏 sample 口径与免责声明 |

---

## 一句话原则

先让部署安全、发布不双发、排名数据可信且可控成本；分析与看板在数据可信之后再加深，不为规划文档里的「完整平台」提前还债。

---

## P4：可靠性与可观测闭环（2026-09-22）

> 在 P0–P3 能力点落地后，补齐「写了但用不起来」与口径/卡死类硬伤。

| 优先级 | 主题 | 目标 |
|--------|------|------|
| P4.1 | 可观测真可用 | 队列读 broker Redis；健康检查探 DB/Redis；前端运维页 |
| P4.2 | 发布与大屏口径 | posting 超时回收 + 强制失败；Dashboard 读 Reddit 表 |
| P4.3 | 生产安全与 CI | 注册开关、生产 env 校验、CORS 白名单、GitHub Actions |
| P4.4 | 文档 | 使用手册与侧栏/路由一致；本清单 |

| 完成 | ID | 备注 |
|------|-----|------|
| ☑ | 4.1.1 | `celery_redis` + `/ops/queues` 读 broker DB |
| ☑ | 4.1.2 | `/api/health` checks database/redis/celery_broker |
| ☑ | 4.1.3 | 前端 `/ops` + `api/ops.ts` |
| ☑ | 4.2.1 | `recover_stale_posting` Beat + force-fail API/UI |
| ☑ | 4.2.2 | `dashboard.py` RedditPost/Comment 统计 |
| ☑ | 4.3.1 | `ALLOW_REGISTRATION` 默认 false |
| ☑ | 4.3.2 | `validate_production` + CORS_ORIGINS |
| ☑ | 4.3.3 | `.github/workflows/ci.yml` |
| ☑ | 4.4.1 | `使用手册.md` 更新 |
| ☑ | 4.4.2 | 本 P4 清单 |
