新引入的问题（按严重度）
N1 reddit_style.py 模块级全局状态（最严重）——_pattern_hits: Counter / _match_samples 是进程内全局变量，且 record_detection_patterns(signs_of_ai) 签名里根本没有 site_id 参数。这意味着：① Celery 多 worker 下各存一份，Beat 写的 Worker 读不到；② 重启即丢；③ 无时间衰减；④ A 站点失败稿的原文片段（可能含品牌名/产品名）会被注入 B 站点的 system prompt——跨租户数据泄漏。

N2 品牌闸 continue 时 revision_notes 不更新——第一轮撞品牌名跳过，第二轮仍是空 notes，等于退化为盲写。

N3 品牌闸撞车误报 likely_ai——实际该提示"删品牌名"，却标记成"文风像 AI"，审核者会改错方向。

N4 find_similar_on_site 口径问题——用 created_at（与 mix 的 published_at 不一致，未发布草稿会进查重池）、order_by(id.desc()) 而非时间、limit(60) 会漏、仍用 difflib 字符级、返回 str|None 丢失相似度信息。

N5 opportunity_score 权重失衡——200 条回复热帖仅扣 1 分，仍能靠"问句+新鲜"拿到 5 分排前面。且 prefer_product_relevant 里先 sorted 再 rank_by_opportunity 重排，前一步冗余。

N6 typo_pairs_for_seed(seed=None)——Random(None) 用系统熵，每次抽不同 4 对，"账号指纹"失效且不可复现、无法测试。

N7 fetch_community_examples 无缓存——每次生成拉一次 Reddit feed，加剧限流风险；建议 Redis TTL 1h 或把 I/O 移出 reddit_style 保持纯函数。

N8 reddit_content 只留最后一轮分数——多轮重写的收敛过程丢失；另需监控"humanize 反而提高 AI 分"的负收益样本。

下一步优先级
N1（落 Redis + 按 site 分桶）→ N2/N3（品牌闸两个漏洞）→ N4（口径统一）→ N5（权重）→ N6-N8 → 时区

一句话总结：这轮修改把"能力缺口"基本补齐，质量很高；但新增的 reddit_style.py 引入了无站点隔离的全局状态，在 Celery 多进程 + 多租户场景下有跨站点数据污染与失效风险，是当前最该优先处理的问题。