二、四个高优先级优化点（全部有代码依据）
🔴 P1：重写是"盲写"——这是最大且最容易修的浪费
代码证据：第 N 轮重写时，调用的是和第一轮完全相同的 ai.generate_reddit_comment(...)，参数一字未变。也就是说——

模型根本不知道上一稿为什么被判为 AI。

而你的 ai_detector.py 明明已经产出了可执行的诊断：

signs_of_ai.patterns[].suggestion（每条模式的修改建议）

signs_of_ai.patterns[].matches[]（命中的具体片段）

lmscan.high_risk_sentences[]（精确到句子的高风险文本）

这些信息全部被丢掉了。

修法：给 generate_reddit_comment 加一个 avoid / revision_notes 参数，把检测诊断拼进去：

python
detection = detect_ai_content(body)
body = ai.generate_reddit_comment(
    ...,
    revision_notes=build_revision_notes(detection),   # 新增
)
build_revision_notes() 把 patterns[].suggestion + high_risk_sentences 拼成：

"Your previous draft was flagged. Rewrite these specific sentences in plainer words: [...]. Avoid: em dash overuse, rule-of-three lists, conjunctive adverbs."

这是改动最小的最大提升——检测器、重写循环都现成，只差把两端接上。

🔴 P2：一个真实 BUG —— 检测结果被吞掉
python
risk = "ok" if verdict != "mixed" else "ok"
两个分支返回完全相同的值。要么是笔误（本意可能是 "mixed" 单独标记），要么是故意放宽但写法误导。无论如何：

mixed（30–60 分）的内容完全没有任何标记就进了审核队列；

审核人员看到的是"干净"的稿子，不知道它其实是 AI 味偏重的。

建议：GeneratedComment.ai_risk 扩展为 ok | mixed | likely_ai 三态，前端在审核卡片上对 mixed 加黄色提示，让审核者优先重写。保留 rewrite_count 也是好设计，可以进一步要求"重写 ≥2 次仍为 likely_ai 就自动打回"。

🟠 P3：humanize_comment 跑在检测之后 —— 顺序反了
代码证据：

python
verdict = detect_ai_content(body).verdict   # ① 在"未加笔误"的文本上检测
...
body = humanize_comment(body, ...)          # ② 检测完才注入笔误
后果有两个：

检测的是"还没真人化"的稿子，等于用更严的标准判断，可能白白触发重写；

更关键：注入笔误（teh/adn/realy…）和换开头（_OPENER_SWAPS）本身就是在降低 AI 分——但这一步的效果从来没被验证过。你不知道"真人化"到底把分数降了多少。

建议：调整成 humanize → detect，并在 GeneratedComment 里记录 ai_score_before_humanize / ai_score_after_humanize。跑一周就能量化"真人化"的真实收益，决定要不要加码。

🟠 P4：笔误白名单是"矩阵指纹"
代码证据：_TYPO_PAIRS 是硬编码的 12 对：the→teh、and→adn、really→realy、because→becuase……

问题在于：同一站点的多个账号，用同一份白名单、同一个 rng，会产出高度相似的"错误风格"。真人不会都犯一模一样的拼写错误。Reddit 的反作弊如果做跨账号文本相似度，这批"同款错别字"反而是抓手。

建议：

每个账号绑定独立的笔误子集（从扩大的词表里按账号 seed 抽 3-5 对）；

笔误词表扩到 40-60 对，覆盖更多高频词；

引入账号级"口音"——比如某号习惯用小写、某号爱用 ...、某号爱省略句尾。让每个号有自己的文本指纹，而不是共享一套。

三、中等优先级
🟡 P5：ai_writer.py 提示词很丰富，但缺 two 样东西
你的提示词库其实质量相当高——REDDIT_VENT_PROMPT / UNPOPULAR / GUIDE / HELP_SEEK / AUTO / PITFALL 六种形态，_COHERENCE_RULES 强调"标题正文同一故事""必须属于该 sub"。这是亮点。

缺的是：

缺失	说明
社区 few-shot	prompt 里说 "match what that community actually posts"，但没有给它任何该社区的真实样例。模型只能靠训练记忆猜。抓 3 条该 sub 的高赞帖贴进 prompt，效果立竿见影
检测器反向赋能	ai_detector 知道哪 12 类模式最容易被识破（em_dash_overuse、rule_of_three、chatbot_phrase…），但 prompt 只泛泛说"never use em dashes"。应把最近高频命中的 matches 词动态加进 system prompt 的 negative examples
🟡 P6：发现层的机会判断
reddit_discover.py 的 filter_commentable 用 min_score: int = 0（等于不过滤），pick_discover_targets 是纯 rng.sample 随机抽社区。建议：

社区改加权抽样（历史回应率高的多抽）；

帖子加机会分 = 问句加分 + 新鲜度 + 评论数少加分 − 已有大量回复扣分；

relevance_score 现在是纯子串匹配 t.lower() in text，改成词边界匹配，避免 "car" 命中 "career"。

🟡 P7：配额与查重的口径问题
文件	问题
reddit_mix.py	count_mix_window 用 created_at >= since 筛选，但配额应按发布时间算——8 天前创建、今天发布的帖会被漏算
reddit_risk.py	查重 difflib.SequenceMatcher 0.85 仅限同账号近 7 天。矩阵号之间撞车、与社区已有内容撞车，都查不到
reddit_mix.py	7 天 10% 上限在总量小时过度严苛（发 3 条后一条 promo 都不能发）。建议改「max(2, 总数×10%)」双轨
全项目	datetime.utcnow() 已在多处（reddit_mix / reddit_risk / serp_provider）deprecated，且 get_account_daily_usage 用 UTC 零点算"当日限额"，但运营者在北京时间——体验错位